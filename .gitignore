# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# playwright
/playwright-report
/test-results
/playwright/.cache

# react-router / vite build
/build
/.react-router/

# cloudflare
/.wrangler/
.dev.vars*
!.dev.vars.example
worker-configuration.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
.pnpm-debug.log*

# env files — 실제 secret 파일만 무시, `.env.example`/`.dev.vars.example`은 커밋 유지.
.env
.env*.local

# supabase CLI (컷오버 완료 후 supabase/ 와 함께 제거 예정)
supabase/.temp
supabase/.branches

# typescript
*.tsbuildinfo

# scratch artifacts for the quiz-generate workflow (prepared prompts, sub-agent outputs, reviewer verdicts).
.cache/
