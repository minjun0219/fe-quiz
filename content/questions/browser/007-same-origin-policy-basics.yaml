id: browser-007
category: browser
difficulty: easy
type: single_choice
question: 브라우저의 동일 출처 정책(Same-Origin Policy)에서 두 URL이 **같은 출처**로 간주되려면 무엇이 모두 일치해야 할까요?
choices:
  - id: a
    text: 스킴(scheme), 호스트(host), 포트(port)가 모두 같아야 해요
  - id: b
    text: 호스트(host)만 같으면 같은 출처로 봐요
  - id: c
    text: 도메인의 최상위(TLD)만 같으면 같은 출처로 봐요
  - id: d
    text: 경로(pathname)와 쿼리스트링까지 모두 같아야 해요
answer: a
explanation: 출처(origin)는 `scheme` + `host` + `port` 세 가지의 조합으로 정의돼요. 예를 들어 `https://example.com`과 `http://example.com`은 스킴이 달라서 다른 출처고, `https://example.com:443`과 `https://example.com:8443`은 포트가 달라서 다른 출처예요. 경로나 쿼리스트링은 출처 판정에 들어가지 않아요. 같은 출처 사이에서는 `fetch`·DOM 접근·스토리지 등이 자유롭지만, 다른 출처 자원에 접근하려면 CORS 같은 별도 메커니즘이 필요해요.
references:
  - title: MDN — Same-origin policy
    url: https://developer.mozilla.org/en-US/docs/Web/Security/Same-origin_policy
  - title: MDN — Origin
    url: https://developer.mozilla.org/en-US/docs/Glossary/Origin
  - title: HTML Standard — Origin
    url: https://html.spec.whatwg.org/multipage/browsers.html#origin
tags:
  - security
  - same-origin-policy
  - origin
  - cors
  - web-platform
