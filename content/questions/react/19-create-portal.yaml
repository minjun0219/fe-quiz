id: react-019
category: react
difficulty: medium
type: single_choice
question: |
  모달을 `createPortal`로 `document.body` 아래에 마운트하는 이유로 가장 정확한 것은?
code: |
  import { createPortal } from 'react-dom'

  function Modal({ children }) {
    return createPortal(
      <div className="modal">{children}</div>,
      document.body,
    )
  }
choices:
  - id: a
    text: "React 컴포넌트 트리(이벤트 버블링, context)는 그대로 부모를 따르지만 DOM 위치만 body로 옮겨서 조상의 `overflow: hidden`이나 stacking context 영향을 회피해요"
  - id: b
    text: "portal로 옮긴 노드는 React 트리에서도 분리돼서 부모 context를 받지 못해요"
  - id: c
    text: "성능상 body에 마운트하는 게 빨라서 일반 컴포넌트보다 리렌더가 적어요"
  - id: d
    text: "portal은 이벤트가 React 트리를 거치지 않고 DOM 트리만 따라가요"
answer: a
explanation: |
  Portal의 핵심은 "DOM으로는 다른 곳에 그리고, React 입장에선 여전히 그 자리에 있는 것처럼 동작"이에요. 그래서 context를 그대로 구독하고 이벤트도 React 트리를 따라 버블링돼요.
  상위의 `overflow: hidden` / `transform`/`z-index` 때문에 모달/툴팁이 잘리거나 다른 요소 뒤에 숨는 일을 막을 때 포털이 제격이에요.
  b/d는 포털 동작과 정반대이고, c는 성능 주장이 근거가 없어요.
tags: [ portal, modal, dom, context ]
